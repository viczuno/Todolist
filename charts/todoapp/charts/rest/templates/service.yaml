apiVersion: v1
kind: Service
metadata:
  name: {{ include "rest.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.global.rest.service.port }}
      name: rest-http
      protocol: TCP
      targetPort: {{ .Values.global.rest.service.port }}
  selector:
    app: {{ .Chart.Name }}
    release: {{ .Release.Name }}