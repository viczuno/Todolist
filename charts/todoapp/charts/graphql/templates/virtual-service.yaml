apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "graphql.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
    - "*"
  gateways:
    - ingress-gateway-configuration
  http:
    - match:
        - uri:
            prefix: "/query"
      route:
        - destination:
            host: {{ include "graphql.fullname" . }}
            port:
              number: {{ .Values.global.graphql.service.port }}