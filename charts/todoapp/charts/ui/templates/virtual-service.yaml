apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "ui.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
    - "*"
  gateways:
    - ingress-gateway-configuration
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: {{ include "ui.fullname" . }}
            port:
              number: {{ .Values.global.ui.service.port }}